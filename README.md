# Getting started

To launch the project, simply run `docker compose up` in the root directory of the project.

The API will be available at `http://localhost:3000`.
You can also inspect the db via mongo express at `http://localhost:8081` (username: `admin`, password: `pass` by default).